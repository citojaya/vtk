# vtk scripts for PARAVIEW simulation
run_par.py <particle.dat> <start_frame> <end_frame>
particle.dat - generated by DEM simulation which contains particle data
start_frame - starting frame
end_frame - ending frame


#vtk scripts for creating surfaces and volumes

Create geometry in Ansys and export in *.cgns format
Open *.cgns file in SALOME 

Use Compound Mesh feature to extract surfaces or volume

Export as a GMF file

Convert mesh file to dat file(surface.dat) which will be used later for creating vtu file
salome_to_vtk.py <surface.mesh>

#Build vtu file (surface.vtu) from triangle data (surfaces only)
and write faces on to a file (*-mesh.dat) which is used in the simulation to calculate contact force with wall
use -m option to write *-mesh.dat (otherwise it will not be writtne)
vtk_from_triangle.py <surface.dat> <scalefactor>

################################################################
# Collision frequency and collision energy probability distribution
python collision.py <particle.dat> <outfile.dat> <frame>
Read from particle.dat and plot collision frequency, collision energy, velocity


################################################################
DPI animation

#Rotate capsule and generate snapshots
python rotate_cap.py ../../Documents/dpi-test4/capsule-wall.dat <angVel> <endframe>

#Build PVD file for capsule animation
python run_drum.py ../../Documents/dpi-test4/capsule <startframe> <endframe>

